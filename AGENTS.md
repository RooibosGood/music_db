# プロジェクト基本ルール (Project Rules)

本プロジェクトにおける開発・運用・エージェント実行に関する基本ルールです。

## 1. スクリプト実行ルール

| スクリプト | 実行可否 (本マシン) | 説明・備考 |
| :--- | :--- | :--- |
| `voice_bot.py` | ❌ **実行禁止 (通知必須)** | Jetson Orin Nano Super で実行するため、本マシン（ローカル環境）では実行しないでください。動作確認や実行が必要な場合は、**ユーザーに Jetson 上で実行するよう通知・依頼** してください。 |
| `build_music_db.py` | ⭕ **実行可能** | 本マシンで実行するため、実行してもよいです。 |
| `cleanup_long_tracks.py` | ⭕ **実行可能** | 本マシンで実行するため、実行してもよいです。 |
| `fix_descriptions.py` | ⭕ **実行可能** | 本マシンで実行するため、実行してもよいです。 |
| `populate_english_names.py` | ⭕ **実行可能** | 本マシンで実行するため、実行してもよいです。 |
| `scan_genres.py` | ⭕ **実行可能** | 本マシンで実行するため、実行してもよいです。 |

## 2. ディレクトリ・仕様書・ファイル管理ルール

- **仕様書の同期更新**: プログラムや機能、UI、API、データ構造に変更を加えた場合は、**必ず `doc/` ディレクトリ配下の仕様書（`VOICE_BOT_SPEC.md`, `DB_SPEC.md` 等）もあわせて更新** してください。
- **Git Commit & Push**: 作業完了時や機能実装・修正時は、変更内容（コードおよび仕様書）を **Git commit & push** してください。
- **`doc/`**: 各プログラムの仕様書を格納するディレクトリです。仕様書や設計ドキュメントはここに配置します。
- **`README.md`**: プロジェクトの概要や最初に読むべき重要情報を記載しています。
- **`music_meta.db`**: 音楽データから作成した SQLite データベースです。**Git に commit しないでください**（`.gitignore` 対象）。

